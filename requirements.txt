# Pinned to the versions used to produce the results in this replication.
# Experiments were run on Windows with an NVIDIA RTX 3070 (8 GB), Python 3.12,
# PyTorch built against CUDA 12.4.

# Install PyTorch with the CUDA 12.4 build from the PyTorch index, e.g.:
#   pip install torch==2.6.0+cu124 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124

transformers==5.12.1
bitsandbytes==0.49.2
accelerate==1.14.0
datasets==5.0.0
numpy==2.4.6

# Used for the binomial significance tests (analysis only, not model inference).
# scipy was not in the experiment environment; any recent version reproduces
# scipy.stats.binomtest, which is stable across versions.
scipy>=1.11

# Figures.
matplotlib>=3.8
